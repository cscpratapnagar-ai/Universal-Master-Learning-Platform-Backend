spring:
  application:
    name: universal-master-learning-platform-backend
  profiles:
    default: local

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
