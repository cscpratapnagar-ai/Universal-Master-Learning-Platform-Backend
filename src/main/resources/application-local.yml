spring:
  main:
    banner-mode: console

  datasource:
    url: jdbc:postgresql://localhost:5433/master_learning
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

app:
  security:
    jwt:
      secret: ${JWT_SECRET:MySuperSecureJWTSecretKeyForMasterLearningPlatform2026!ChangeMe}
